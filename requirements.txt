fastapi<0.137.0
uvicorn[standard]
sqlalchemy
psycopg2-binary
python-dotenv
apscheduler
jinja2
requests
httpx
# Assume NemoClaw/OpenClaw SDK exists or we use standard LLM libraries. We can add it here if there's a specific package.
# nemoclaw
# openclaw
python-multipart
prometheus-client
prometheus-fastapi-instrumentator
python-logging-loki
